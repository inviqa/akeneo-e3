resource-type: product # options: product-model, family, any supported API aliases

# Data source
data-source-type: api # options api, csv, xlsx?

# filter for the API call (API related?)
conditions:
    -
        field: 'identifier'
        operator: 'IN'
        value: ['100000007']

# Upload strategy?
upload-mode: patch

actions: []

# (optional) API related? or also csv?
upload:
    batch-size: 100
