# Expression functions

Expressions allow manipulating data using functions, like in Excel/Google sheets.

{% for functionName, function in functions %}

## {{ functionName }}

{{ function.summary }}

{% if function.examples|length > 0 %}

Examples:

{% for example in function.examples %}
 * `{{ functionName }}({{ example.arguments|raw }})` => `{{ example.result }}`
{% endfor %}
{% endif %}

{% if function.parameters|length > 0 %}

| Parameter | Description | Type
| --------- | ----------- | ----
{% for parameter in function.parameters %}
| {{ parameter.name }} | {{ parameter.description }} | {{ parameter.type|replace({'|': '/'}) }}
{% endfor %}
{% endif %}

{{ function.description|raw }}

{% endfor %}

